
@model List<Quiz>   

    <div class="card shadow border-0 mt-4">
        <div class="card-header bg-secondary bg-gradient ml-0 py-3">
            <div class="row">
                <div class ="col-12 text-center">
                    <h2 class="text-white py-2">Select category</h2>
                </div>
            </div>
        </div>
        <div class="card-body p-4">
        <div class="row pb-3">
            <div class="col-6">
                
            </div>
           @*  <div class="col-6 text-end">
                <a asp-controller="quiz" asp-action="create" class="btn btn-primary">
                    <i class="bi bi-plus-lg"></i> add new topic
                </a>

            </div> *@
        </div>
        <table class="table table-bordered table-striped mt-1">
            <thead>
                <tr>
                    <th>
                        sr.no
                    </th>
                    <th>
                        topic
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @{
                    //razor syntax
                    int srno = 1;  // initialize the serial number
                }
                @foreach (var obj in Model)
                {
                    <tr>
                        <td>
                            @srno
                        </td>
                        <td>
                            @obj.Topic
                        </td>
                        <td>
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Test" asp-action="TakeTest" asp-route-id="@obj.quiz_id" class="btn btn-primary mx-2">
                                    <i class="bi bi-pencil-square"></i> Attend
                                </a>
                               @*  <a asp-controller="quiz" asp-action="delete" asp-route-id=" @obj.quiz_id" class="btn btn-danger mx-2">
                                    <i class="bi bi-trash"></i> delete
                                </a>
                                <a asp-controller="question" asp-action="index" asp-route-id="@obj.quiz_id" asp-route-topic="@obj.Topic" class="btn btn-primary mx-2">
                                    <i class="bi bi-plus-lg"></i> question
                                </a> *@
                            </div>
                        </td>
                    </tr>
                    srno++;
                }
            </tbody>
        </table>

        </div>

    </div>
  


